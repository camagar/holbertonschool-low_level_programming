#include "holberton.h"

/**
 * _strlen - check the code for Holberton School students.
 * @s: variable to calculate length
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; *(s + i) != '\0'; i++)
	{
	}
	return (i);
}
